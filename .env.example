# Example environment variables for local development / deployment
# Copy this file to .env.local (for local dev) or set matching variables in Vercel project settings

# The GS1 API key used by app/api/vbg-lookup
GS1_API_KEY=
